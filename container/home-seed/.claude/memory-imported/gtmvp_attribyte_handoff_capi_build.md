---
name: gtmvp-attribyte-handoff-capi-build
description: "The GTMVP→Attribyte handoff (4 plays) + Tracking Health + CAPI relay — 4 PRs shipped 2026-06-02, what's merged-ready vs held, and the deferred items."
metadata: 
  node_type: memory
  type: project
  review_after: 2026-06-15
  originSessionId: 19628f7d-c9d4-4465-b2ba-b6931891789e
---

Built autonomously 2026-06-02 ("build all 4 + the tracking/CAPI capability, without my review, ultracode"). Design docs: `Downloads\gtmvp-attribyte-handoff-design.md` + `Downloads\attribyte-tracking-capi-design.md`.

**MERGE STATUS — ALL 4 MERGED (2026-06-02):** #254 (GTMVP) + #153 + #154 + #155 all merged to main. **All 3 migrations applied + verified live on prod `kqpllsenlvdgsznptylb`:** `cpa_guarantee_snapshots`, `events.event_id`(+index), `conversion_relays`. #154 conflict was only `apps/api/src/index.ts` (kept both routers). #155 was rebased onto main (`git rebase --onto origin/main f9d26c9` dropped the squashed PR3 commit, kept PR4) — clean, build-verified, squash-merged. **Meta CAPI is live in code but won't FIRE until a `pixel_id` + system-user token are set via `POST /integrations/conversion-config` (per workspace).** The full GTMVP→Attribyte handoff + tracking + Meta CAPI capability is shipped end-to-end.

**4 build-verified PRs (each isolated worktree off origin/main, build green):**
- **GTMVP/GTMVP_V0 #254** — `lib/attribyte/handoff-profile.ts` pure composer emits a canonical diagnostic snapshot (`businessProfile`) on the provision call. 16 vitest + tsc green. *Safe to merge; deploy AFTER Attribyte #153 accepts the field.*
- **Stevekaplanai/attribyte #153** — ingest `businessProfile` into `workspaces.settings.business_profile` + the 4 plays: Atlas `get_business_context`/`compare_cpa_to_baseline` tools, CPA-guarantee proof engine (`lib/cpa-guarantee-measure.ts` + `cpa_guarantee_snapshots` migration), `GtmDiagnosticPanel` + `/business-profile` route, cache-priming on connect/spend. 8/8 turbo.
- **Stevekaplanai/attribyte #154** — tracking foundation: `events.event_id` (sdk-web + sdk-node mint + persist + migration), Tracking Health scorecard (`health/tracking-health-service.ts` + route + `TrackingHealthScorecard` on IntegrationsPage), Tier-A tag snippet + Tier-B GTM export. **HELD for Steve's merge** (customer-facing). 8/8 turbo.
- **Stevekaplanai/attribyte #155** — CAPI relay scaffold + **Meta = live reference** (`conversions/capi-relay-orchestrator.ts` + `capi-payload-builders.ts` + Meta `pushConversions` + `conversion_relays` migration + `/integrations/conversion-config`). **STACKED on #154** (base = its branch). **HELD for Steve's merge.** 8/8 turbo.

**Honesty guardrail (load-bearing throughout):** profile = `gtmvp_diagnostic` analysis, stored in `settings` (never a metric row, never sample-marked), rendered only behind violet "From your GTM diagnostic" vs blue "measured by AttriByte"; null measured CPA = "not yet measurable", never 0; tracking scorecard keeps pixel-firing / CAPI-configured / CAPI-live distinct; sample fallbacks carry a "Sample" badge. Relay fires only with `event_id` (dedup-safe) + marketing consent.

**Deploy ordering (flagged in PRs):** Attribyte ingestion (#153) before GTMVP emitter (#254) sends; apply `event_id` + `conversion_relays` + `cpa_guarantee_snapshots` migrations before the API deploys; merge #154 before #155. Migrations were NOT applied to prod by me (hard-stop bar) — they ship in the PRs.

**Deferred / scoped out:**
- Design Play 2a (sample-seeder label biasing) — deliberately skipped: handoff customers' workspaces don't run the onboarding seeder, so it adds no handoff value while risking the new-user first impression. Panel (2b) is the real value and shipped.
- Phase 2 CAPI networks (Google OCI/EC, LinkedIn, TikTok) — need live ad creds; scaffolded, `status:'skipped'` until adapters implement `pushConversions`.
- **⚠ Google dev-token deadline 2026-06-15** — `UploadClickConversion` fails for a dev token that never sent OCI/EC. Dual-channel reminder set: GCal event 2026-06-10 on steve@stevekaplan.ai + (session) cron. Only matters IF the Google leg is built.
- **Google CAPI leg = PATH A — CODE BUILT (PR #156, 2026-06-02, OPEN/held for Steve's merge).** Path A = Pipedream supplies OAuth (existing `resolveCredentials`); dev token = `GOOGLE_ADS_DEVELOPER_TOKEN` env (adapter already reads it); customer id parsed from the `conversion_action` resource name. NOT a GCP service-account. `google-ads-adapter.pushConversions` → `customers/{id}:uploadClickConversions` (OCI/Enhanced Conversions), dedup `orderId=event_id`, dormant until dev token + conversion_action set (mirrors Meta's pixel gate). `validateOnly` (settings `validate_only`) added across types/orchestrator/`/conversion-config` for dry-runs + the dev-token warm-up. **ACTIVATE:** set `GOOGLE_ADS_DEVELOPER_TOKEN` on Railway + `POST /integrations/conversion-config {provider:google_ads, conversionAction:"customers/<id>/conversionActions/<n>", validateOnly:true}` to dry-run+warm before June 15, then `validateOnly:false`. gcloud installed by Steve 2026-06-02 but NOT on the Claude tool's PATH (needs a Claude restart to drive gcloud from the tool side).

**ACTIVATION SESSION (2026-06-02, in progress):**
- Dev token LIVE in Railway (Basic access — confirmed sufficient for OCI; Standard only needed past ~15k ops/day). Pipedream Google Ads connection exists.
- Conversion action = **`customers/1857683050/conversionActions/6939654621`** (from `ctId` in the Google Ads conversion-detail URL; customer 185-768-3050; "Google Shopping App Purchase"). #156 made conversionAction dash-tolerant.
- **PR #157 MERGED (aefadb7): bumped Google Ads API v15→v24.** v15 was SUNSET — every Google call 404'd (`/v15/customers:listAccessibleCustomers`). This had silently broken the WHOLE Google adapter. v24 = latest (sunset ~May 2027).
- **Prod Attribyte DB = `kqpllsenlvdgsznptylb` ("supabase-attr-byteweb") — the ONLY Attribyte Supabase project in the account** (the old memory's `vrivnwhpdubkwhdiglyk` mismatch ref is NOT in this account). API writes here.
- Steve connected Google Ads at app.attribyte.xyz (as steve@stevekaplan.ai, workspace "Acme SaaS (Demo)" 5ab0ffdc-6491-4c21-a310-817005bfbbbc) but it did NOT persist a `google_ads` integration row — the connect validation 404'd on dead v15. **After the v24 deploy lands, Steve must RECONNECT Google Ads in the app** → writes a real row with `pipedreamAccountId` → then set conversion_action + validate_only:true → dry-run/warm → validate_only:false.
- The OAuth'd Google login (steve@) must have access to customer 185-768-3050 (where the conversion lives) or OCI fails on permissions.
- **ROOT-CAUSE BUG FOUND + FIXED (PR #158, open):** the Pipedream Connect flow NEVER persisted ANY integration. Hosted Connect UI opens in a popup; the success redirect (+ any account id) lands INSIDE the popup, not the opener, so the opener never called `/connect-complete` (the only writer of the integrations row). OAuth completed + UI went green, but nothing saved — blocked Google AND Meta AND every integration (only May-29 `_demo` rows existed). Fix: `/connect-complete` `accountId` now OPTIONAL → resolves via `pipedreamService.listAccounts(workspaceId,{provider})` (external_user_id=workspaceId); MERGES settings (was full-replace, which clobbered conversion_action on reconnect); clears `_demo`. Frontend: popup-close calls `/connect-complete {provider}`; popup auto-closes on success. Recovers already-connected accounts (no re-auth). **After #158 deploys: Steve clicks Connect once → row persists with pipedreamAccountId → then set conversion_action + dry-run.**
- Live API confirmed: SUPABASE_URL = kqpllsenlvdgsznptylb (my migrations ARE on the live DB — event_id safe). API is current (business-profile + tracking-health routes return 401 = deployed). Tracking Health scorecard renders live (Sample fallback is client-side hardcoded, so it shows even if API errors — not proof of API).
- **After #158: Google Ads integration PERSISTS** (workspace 5ab0ffdc, pipedreamAccountId `apn_QPhL58B`, real not demo, v24). NEXT BLOCKER: OAuth token from Pipedream resolves EMPTY → Google 401 `CREDENTIALS_MISSING` on ListAccessibleCustomers. **PR #159 MERGED:** tolerant token extraction (firstStringField over oauth_access_token/access_token/accessToken/token) + safe admin diagnostic `GET /integrations/:provider/connection-debug` (returns credentialKeys + hasAccessToken, NO secret values). Diagnostic (#159 /connection-debug) returned `credentialKeys: []`, `hasAccessToken: false`, account found+healthy. **ROOT CAUSE (confirmed via Pipedream docs):** retrieving raw connected-account credentials via API is NOT supported with Pipedream's DEFAULT OAuth client — only with a CUSTOM (bring-your-own) OAuth client. Google was connected via the default client → Pipedream returns empty credentials → relay can't get a token. NOT a code bug. **FIX (Steve config, no code):** (1) create a Google Cloud OAuth 2.0 Web client (scope `https://www.googleapis.com/auth/adwords`, add Pipedream's redirect URI); (2) register it as a CUSTOM OAuth client for the `google_ads` app in the Pipedream project (proj_VbsZ3BV); (3) reconnect Google Ads in Attribyte. Then /connection-debug → hasAccessToken:true → relay authenticates → set conversion_action + dry-run. **THIS APPLIES TO EVERY NETWORK the relay forwards to (Meta/LinkedIn/TikTok too)** — each needs a custom Pipedream OAuth client for raw-token export, OR rewrite adapters to use Pipedream's proxy (bigger change). This is what the gcloud/GCP OAuth-client work is actually for (NOT a service account). Possible small code follow-up: pass `oauth_app_id` (custom client id) in createConnectToken if Pipedream doesn't auto-use the project's custom client on reconnect. PRs #156/#157/#158/#159 MERGED.
- **RESOLVED 2026-06-02 ~17:47:** Steve registered a custom Google OAuth client in Pipedream + added himself as a Test user (External/Testing consent screen) → reconnected → new account `apn_EOh1Vdk` (custom client), **status=active, token exports** (proven: ListAccessibleCustomers succeeded). The auto-use of the project's custom client worked on reconnect — NO oauth_app_id code change needed. Set on the google_ads integration (workspace 5ab0ffdc) via direct DB write (no UI form): `conversion_action=customers/1857683050/conversionActions/6939654621`, `validate_only=true`. **Google CAPI relay fully wired + configured in DRY-RUN.** then flip validate_only→false for live. The custom-OAuth-client requirement applies per-network (Meta/LinkedIn/TikTok each need one for the relay).
- **GOOGLE CAPI PROVEN END-TO-END 2026-06-02 ~18:42.** Root resolution chain: Pipedream's DEFAULT OAuth client never exports tokens → registered a CUSTOM Google OAuth client in Pipedream (id `oa_Wyiyxk`) → wired `oauthAppId` into the Connect Link (PR #161) read from env `PIPEDREAM_OAUTH_APP_ID_GOOGLE_ADS=oa_Wyiyxk` (Steve set on Railway) → clean reconnect created custom-client account `apn_OghkvW3` which DOES export the token. Test-conversion endpoint (PR #160, `POST /integrations/:provider/test-conversion`) returned **httpStatus 200**, only error = the dummy gclid format (`TEST_GCLID_DO_NOT_ATTRIBUTE`) — i.e. auth + dev token + conversion_action `customers/1857683050/conversionActions/6939654621` + customer access ALL valid. Relay authenticates + Google validates the upload. **8 PRs total: #153-#161.** validate_only:true (dry-run); flip to false to record real conversions. GOTCHA: conversion_action wiped on each disconnect+reconnect (the Attribyte disconnect deletes the row → connect-complete has nothing to merge) — re-set via DB each time; tell Steve NOT to use Attribyte's disconnect. Real relaying needs the Attribyte tag on a live site capturing real gclids + a conversion_goal. Meta/LinkedIn/TikTok = repeat the custom-OAuth-client pattern.
- **2026-06-02 ~19:00:** real-gclid test (gclid from fabricmegastore.com) → httpStatus 200, gclid ACCEPTED (no gclid error → fabricmegastore IS customer 1857683050), only error = "The conversion action specified in the upload request isn't set up for uploading conversions." The conversion action 6939654621 ("Google Shopping App Purchase") is a WEBSITE/Shopping conversion — OCI requires an **Import**-source conversion action. FIX (partner, Google Ads): Goals→Conversions→New→**Import** ("conversions from clicks", manual API/upload) → get its numeric ID → update settings.conversion_action to it → re-test → fully green. Auth + token + gclid + account match all CONFIRMED working; only the conversion-action TYPE remains.
- **2026-06-02 ~19:10 — FULLY PROVEN (pending Google's clock).** Created Import-source conversion action "Attribyte CAPI – Purchase" = `customers/1857683050/conversionActions/7633135907` (fabricmegastore), updated settings.conversion_action to it. Real-gclid test → httpStatus 200, final error = **"Can't import events to a conversion action that was just created. Try importing again in 6 hours."** = Google FOUND + accepted everything; only its 6-hour new-conversion-action cooldown remains. NOTHING left to fix. **Calendar reminder set (steve@stevekaplan.ai, 2026-06-03 9am ET):** re-fire the test snippet (expect success:true), then Claude flips settings.validate_only→false + Steve re-fires once with the real gclid → records a real conversion on the dedicated test action + WARMS the dev token before the 2026-06-15 OCI→Data-Manager cutoff. Connected account stays apn_OghkvW3; don't reconnect (would wipe conversion_action + need re-set). GOOGLE CAPI = DONE pending the 6h clock.
- **2026-06-02 — STRATEGIC PIVOT (Steve's call): for SHOPIFY clients, LEAN ON SHOPIFY-NATIVE TRACKING; Attribyte does NOT relay purchases for them.** Discovery that prompted it: the client's "Shopify connected" is the store wired to Google/Meta *natively* (Shopify's Google & YouTube + Facebook/Instagram sales channels — that's where the "Google Shopping App Purchase" action came from); **Attribyte has ZERO Shopify code**, and the relay only fires when a conversion ENTERS Attribyte (SDK touchpoint w/ event_id) — nothing feeds it for this client. Shopify already pushes purchases to Google AND Meta server-side, so the relay would be redundant + risk double-counting. **Decision:** Attribyte = the cross-channel ATTRIBUTION + MEASUREMENT + CPA-GUARANTEE layer ON TOP of native tracking, NOT the purchase relayer, for Shopify clients. **Consequences:** (1) Do NOT build Shopify order ingestion. (2) Do NOT build the SDK-on-storefront path. (3) Do NOT build the Google auto-conversion-action resolver (it's part of the relay path — not needed here; I paused mid-build, correctly). (4) The merged relay code STAYS — dormant for Shopify clients, available for non-Shopify (SDK/CRM) clients later; don't rip it out. (5) The June-15 dev-token warm-up is now MOOT for this client (we're not uploading its conversions); BUT the Google Ads API connection still matters for DATA READS (spend+conversions feed the CPA math) — that path is unaffected by the OCI→Data-Manager migration. **Real next step (replaces the warm-up):** confirm Attribyte is SYNCING fabricmegastore's Google Ads spend+conversions so the CPA-guarantee math renders, + connect Meta for the cross-channel view. **Honest limitation of this path:** cross-channel attribution is constrained to platform-reported (per-channel, last-click-ish) conversions until a journey source (SDK or order ingestion) exists — fine for blended CPA/ROAS + the guarantee math now; revisit if true multi-touch DDA is needed. Calendar event 30ip29i1p4ffg1jg2ev8gl6suk repointed to this next step.
- **2026-06-02 — LIVE DB DIAGNOSIS (the chosen "measurement layer" path is BLOCKED by a broken Google sync).** Verified prod `kqpllsenlvdgsznptylb`: the ONLY workspace is `5ab0ffdc…bbbc` = **"Acme SaaS (Demo)"** (fabricmegastore's real Google account was wired into this DEMO workspace just for the CAPI test). **All 207 `ad_spend` rows are `provider:'seed'`** ($53,711 fake spend, 0 conversions, channels "Paid Search/Paid Social/Display Retarget") — the dashboard is 100% seeded demo data. `cpa_guarantee_snapshots`=0, `events`=0. The Google Ads integration auto-syncs every ~60s (API `setInterval` poller `integration-sync-runner.ts`; worker's `integration-sync.ts` is a STUB) and **FAILS every time** for 2 reasons (`sync_logs`): (1) **`UNRECOGNIZED_FIELD`** — `fetchCampaigns` GAQL selects `campaign.start_date`/`campaign.end_date`, rejected by v24 (google-ads-adapter.ts:284-285). (2) **`REQUESTED_METRICS_FOR_MANAGER`** — `listAccessibleCustomers` returns a MANAGER (MCC) account; metrics can't be queried on a manager. `fetchCampaigns`/`fetchAdPerformance` already accept `options.customerId` (:269,:324) but `runSyncType` never passes one → always hits the manager. `googleAdsQuery` has NO `login-customer-id` header support. **3rd gap:** `conversion_value` is fetched (`metrics.conversions_value`) but DROPPED — no column in `ad_spend` (only `conversions` count) → ROAS uncomputable from this path; CPA OK. **FIX (designed, NOT yet built):** pass `settings.customer_id` (+ optional `settings.login_customer_id`) from runSyncType into the adapter; add `login-customer-id` header to googleAdsQuery; remove the 2 rejected campaign fields (normalizeCampaign tolerates); add a `conversion_value` column + map it. **GATING DECISIONS (asked Steve 2026-06-02):** (a) wire which workspace live — recommend a CLEAN real fabricmegastore workspace, NOT polluting the Acme demo; (b) need the 10-digit MANAGER/MCC account ID above fabricmegastore for login-customer-id (evidence-bar — only Steve can see it). fabricmegastore client account = 1857683050.
- **2026-06-02 — PR #162 SHIPPED (held for Steve's merge) + the BIGGER finding.** PR #162 (`claude/fix-google-ads-sync-mcc-FS6Lj`, build-verified) fixes the sync: threads `settings.customer_id` + `settings.login_customer_id` through runSyncType→fetchCampaigns/fetchAdPerformance, adds `login-customer-id` header to googleAdsQuery, drops the v24-rejected `campaign.start_date/end_date`, rounds fractional conversions for the integer column. **PIN ALREADY SET in prod** on the Acme/google_ads integration: `customer_id=1857683050`, `login_customer_id=5511138631` (GTMVP MCC, Steve confirmed). Post-merge+deploy, a manual sync (`POST /integrations/:id/sync`) pulls real spend (status is currently 'error'→manual trigger bypasses the due-filter; success flips it active). **THE BIGGER FINDING (gates whether the chosen path shows anything): the CPA-guarantee "Measured" CPA counts CONVERSIONS from `touchpoints.is_conversion=true` (cpa-guarantee-measure.ts:76-89), NOT from `ad_spend.conversions`.** Touchpoints come from SDK/CRM/reconstruction. The "lean on Shopify-native" path creates ZERO conversion touchpoints → fixing the sync gets SPEND but Measured CPA stays "Not yet measurable" forever. **DECISION PENDING (asked Steve, contractual-metric change): point the CPA/ROAS conversions source at platform-reported ad-network conversions (`ad_spend.conversions`, which Google got from Shopify) when there are no first-party touchpoints — the real "lean on native" wiring.** Also pending: convert "Acme SaaS (Demo)" workspace → fabricmegastore (recommended over a new workspace: no Google reconnect needed; CAPI conversion_action no longer matters since relay is shelved) — requires a DESTRUCTIVE seed-data purge (provider='seed' ad_spend + seeded touchpoints/accounts/contacts/opps/campaigns) → confirm scope before deleting prod rows (hard-stop bar).
- **2026-06-02 — FULL SEED PURGE DONE + workspace RENAMED (Steve approved "full seed purge").** Workspace 5ab0ffdc renamed "Acme SaaS (Demo)" → **"Fabric Megastore"**; deleted ALL seeded business data (FK-safe order: attribution_results→identity_link_events→identity_merges→anonymous_identities→device_identities→opportunity_contacts→touchpoints→opportunities→creative_performance→contacts→conversion_goals→campaigns→accounts→ad_spend→mmm_*→custom_attribution_models→events). Verified 0 rows in ad_spend/touchpoints/accounts/contacts/opps/campaigns. KEPT: integrations (3, incl. google_ads w/ pin), workspace_members(1), users, api_keys, sync_logs, atlas chats, journey_templates, identity_window_config. **STILL PENDING — Decision 1 (conversion source, contractual): NOT yet answered by Steve.** Until it is, the Measured CPA reads touchpoints (now 0) → stays "Not yet measurable" even after spend syncs. **Also a 3rd prerequisite for the FULL guarantee panel:** it self-hides unless `workspaces.settings.business_profile` exists (source=gtmvp_diagnostic, baseline_cpa/target_cpa) — fabricmegastore has none yet → needs a GTMVP diagnostic handoff (the #254/#153 play) to show Baseline/Target. Remaining sequence: Steve merges #162 → deploy → (if Decision 1=yes) build conversion-source PR + conversion_value column → manual sync (`POST /integrations/:id/sync`, status currently 'error' so manual kick needed) → spend + Google-reported conversions land.
- **2026-06-02 — PR #163 SHIPPED (stacked on #162, held) + Decision 1 EXECUTED in prod config.** PR #163 (`claude/cpa-platform-conversions-FS6Lj`, build-verified): new `lib/conversion-source.ts` `getConversionSource()` ('platform'|'touchpoints', default touchpoints, at `workspaces.settings.conversion_source`); `/metrics/performance` + `measureGuaranteeCpa` count conversions (+ sum value) from `ad_spend` when 'platform' else touchpoints; new nullable `ad_spend.conversion_value` column (migration 20260603020000 + adapter mapping) for ROAS. **PROD CONFIG DONE:** column added live; Fabric Megastore `settings.conversion_source='platform'`. **MERGE ORDER: #162 first, then #163.** **Part B (seed the full guarantee panel) BLOCKED on honesty:** the panel is honesty-gated — business_profile must carry `source='gtmvp_diagnostic'` + `audit_request_id` + real `baseline_cpa_blended`/`target_cpa`. I will NOT fabricate a gtmvp_diagnostic provenance/baseline (cardinal never-fabricate rule). GTMVP MCP `list_my_audits` needs interactive OAuth (unavailable headless), BUT `get_audit` works with a view-URL token. **ASKED STEVE: paste the fabricmegastore GTMVP audit view URL → I pull the real baseline + seed honestly; OR derive baseline from fabricmegastore's real trailing CPA once the sync lands post-#162-deploy (leave Baseline/Target until then).** Measured CPA will compute off platform conversions regardless once data syncs; only Baseline/Target tiles need the real diagnostic.
- **2026-06-02 — PANEL SEEDED (real GTMVP audit, honest) + #162/#163 MERGED.** Steve built a real GTMVP audit for FabricMegaStore (audit_request_id `4a75d640-5ec1-43af-884c-0790849e41c4`); ads NOT connected in GTMVP so `rebuild_baseline_cpa` was null. I pulled the real audit row directly from GTMVP's Supabase (`swrhibqhjmouiyhbwoae`, table `audit_requests`) — the Supabase MCP CAN access both projects — replicated `buildAttribyteHandoffProfile` faithfully, injected Steve's real baseline (blended CPA **$14**, ROAS ~3.45), and wrote `workspaces.settings.business_profile` DIRECTLY onto Fabric Megastore (5ab0ffdc) via SQL (the provision endpoint routes by external_id, which is null → would've made a NEW workspace). Profile: source=gtmvp_diagnostic, schema 1.0, cpa_guarantee_pct=0.2, baseline_cpa_blended=14, target_cpa=11.2, 5 competitors, 12 findings (3 critical/9 opportunity), goals, channels facebook/google/pinterest. **Known GTMVP bug flagged:** `normalizeChannel` only cleans bare "meta"/"google"/"shopify" — realistic labels ("Meta (Facebook/Instagram)","Google Shopping","Pinterest") pass through ugly; I cleaned them on write; fix on GTMVP side later. **#162 + #163 MERGED to main (deploying).** REMAINING: after Railway deploys, fire a manual Google sync (`POST /integrations/{id}/sync` — manual bypasses the status='error' due-filter; on success flips active) → real spend + platform conversions land → Measured CPA computes → full panel (Baseline $14 / Target $11.20 / Measured) live. Panel currently shows Baseline/Target + "Measuring" until then.
- **2026-06-02 — FULL LOOP PROVEN ON REAL DATA. 🎯** Deployed #162/#163, flipped google_ads active → scheduler ran the sync → **ad_performance SUCCEEDED: 181 real rows, 2026-05-03→06-02, spend $34,462, conversions 2,048, conversion_value $120,158.** Measured blended CPA = **$16.83**, ROAS = **3.49** (Steve's eyeball was 3.45 → pipeline validated). Panel now FULLY live on real numbers: Baseline $14 / Target $11.20 / Measured $16.83 (honestly "measuring/not-met" — engagement just started, CPA above target = correct day-one state). **NEW BUG found + fixed (PR #164):** `campaigns` upsert (ON CONFLICT workspace_id,external_id) failed — table lacked that UNIQUE constraint (only PK on id); accounts+opportunities had it, campaigns was the odd one out. Exposed by #162 (fetch now reaches the upsert). Added the constraint to prod + migration 20260603030000 + PR #164; re-flipped sync to re-run clean. **`contacts` has the SAME mismatch** (upsert vs UNIQUE(workspace_id,email)) — latent, will hit CRM contact sync; flagged in #164, NOT fixed (surgical). Google_ads integration_id = 49e17b59-c106-4745-be15-2c90232be2ea.

- **2026-06-03 — META READ PATH CONFIRMED + v24 bump (PR #177, OPEN/held for Steve's merge).** Resumed the CAPI thread on a fresh machine. All CAPI/handoff PRs (#153–#176) are MERGED; ZERO open PRs in the thread before #177. **Correction to the #164 note above:** the latent `contacts` UNIQUE bug was actually FIXED as **#167** (`UNIQUE(workspace_id, external_id)` on contacts) — not still-open. **Verified the Meta READ path end-to-end** (the "lean on Shopify-native, read platform conversions" path — NOT the shelved relay): registry `createAdapter('meta_ads')`→`MetaAdsAdapter` ✓; `MetaAdsAdapter.fetchAdPerformance` extracts spend/clicks/conversions(purchase/lead/omni_purchase from `actions`)/conversion_value/ROAS ✓; `integration-sync-service` maps `meta_ads:['campaigns','ad_performance']`→`upsertAdPerformance`→`ad_spend` is provider-agnostic incl. the #163 `conversion_value` column ✓; `conversion_source='platform'` (already set on FabricMegaStore) reads `ad_spend.conversions`, so Meta conversions feed the cross-channel CPA/ROAS panel ✓. Meta AVOIDS Google's MCC/login-customer-id headache (`/me/adaccounts` returns client accounts directly). **#1 runtime risk FOUND + FIXED:** the Meta adapter was pinned to Graph API **v18.0** (Sept 2023, PAST Meta's ~2yr sunset; current v24/v25) → reads would 404 exactly like the Google v15→v24 sunset (#157). **PR #177** bumps `META_GRAPH_API_VERSION` v18→v24 in `meta-ads-adapter.ts:23` (typecheck green; held for merge). Dormant direct-OAuth v18.0 URLs (`adapters/index.ts:196`, `adapters/types.ts:470`, `routes/integrations.ts:709`) left UNTOUCHED — Pipedream-managed flow, off the live path; flagged in the PR. **Prereq for Meta to actually sync (same as Google):** custom Pipedream OAuth client for Meta + env `PIPEDREAM_OAUTH_APP_ID_META_ADS` on Railway (the `oauthAppId` plumbing is already generic in `pipedream-service.ts:194`), then connect in-app → manual sync. Corrected partner doc written: **`Downloads\attribyte-meta-read-setup.md`** (READ-only; the relay doc `attribyte-meta-capi-setup.md` is SHELVED for FabricMegaStore but kept for future non-Shopify clients). NEXT (Steve config): register a custom Meta OAuth client in Pipedream `proj_VbsZ3BV` (scopes `ads_read`+`read_insights`), set the Railway env, merge #177, connect Meta in-app → Claude verifies `/integrations/meta_ads/connection-debug` `hasAccessToken:true` + fires the sync.

- **2026-06-03 — OAuth-plumbing "fix-it-for-good" (Option B) SCOPED, NOT built (Steve: "just the plan for now").** Plan doc: **`Projects\attribyte\docs\pipedream-proxy-migration-plan.md`** (untracked). The per-network custom-OAuth-client tax exists because Pipedream only exports raw tokens via a CUSTOM client; the **Connect Proxy** removes it — Pipedream injects the user token server-side, so the DEFAULT client works (no `PIPEDREAM_OAUTH_APP_ID_*`, no `oauthAppId` plumbing, no `getAccountCredentials` export). **Recommendation = HYBRID, NOT rip-and-replace:** proxy for net-new networks (Meta/LinkedIn/TikTok); LEAVE Google on its working custom-client path — Google read = `googleAds:searchStream` (streaming) + proxy's 30s cap + no-streaming + a special `googleads.m.pipedream.net` nested-`{method,url,data}`-body convention ⇒ high regression risk, ~zero benefit on a path that already works. Proxy contract (verified vs docs): `POST api.pipedream.com/v1/connect/{proj}/proxy/{urlsafe_base64(targetUrl)}?external_user_id={wsId}&account_id={apn_}`, `Authorization: Bearer {pd_oauth}` (client-creds infra already in `pipedream-service.ts`) + `x-pd-environment`; custom upstream headers only via `x-pd-proxy-` prefix; 30s timeout, no streaming. Effort: Meta ref impl ~0.5–1d (new `pipedream-proxy.ts` + a `PROXY_PROVIDERS` flag in `runSyncType`; adapters LOSE their own token handling). **OPEN DECISION (deferred): (A) custom-client Meta now + proxy refactor later [the Meta custom-client config becomes throwaway] vs (B) proxy-for-Meta first [no throwaway, slightly slower to Meta-live]. Neither chosen; nothing built or connected.**

- **2026-06-03 — Option B BUILT for Meta (PR #178, OPEN/held for Steve's merge).** Steve chose B. Pipedream Connect Proxy reference implementation: new `proxyFetch()` in `pipedream-service.ts` (urlsafe-base64 target URL + `external_user_id`/`account_id` + Bearer + `x-pd-environment` + `x-pd-proxy-` upstream headers; reuses the cached client-creds token); `metaGet()` in meta-ads-adapter branches proxy vs direct, threaded through getAdAccounts/fetchCampaigns/fetchAdPerformance/testConnection (direct token-in-query path unchanged when no proxy); `PROXY_PROVIDERS={meta_ads}` + `buildProxyContext()` in integration-sync-service (proxy providers skip the raw-token resolve, read accountId from `settings.pipedreamAccountId`); `/:id/test` route proxy-aware (fixes a status→error flip that would've stopped scheduled sync); types `ProxyContext` + `SyncOptions.proxy` + optional `testConnection(proxy)`. Google STAYS on the raw-token path (its `searchStream` is streaming; proxy = 30s cap + no streaming). typecheck + vitest (50) green; code-reviewer pass (0 crit/high; 2 medium latent-risk items fixed: undefined creds to reconstruction in proxy mode + sentinel creds in test route). **CONSEQUENCE — Meta onboarding CHANGED: once #178 merges, Meta connects via PLAIN Pipedream Connect (DEFAULT client) — NO custom OAuth client, NO `PIPEDREAM_OAUTH_APP_ID_META_ADS` env. The custom-client doc `Downloads\attribyte-meta-read-setup.md` is SUPERSEDED for Meta** (still valid for Google, which keeps its custom client). Runtime validation after merge+deploy: connect Meta plain → `POST /integrations/{id}/test` (expect success) → `POST /integrations/{id}/sync` → Meta spend/conversions land in `ad_spend` → cross-channel CPA/ROAS panel includes Meta. To extend to LinkedIn/TikTok later: add to `PROXY_PROVIDERS` + give each adapter a proxy-aware GET reusing `proxyFetch`. Plan doc: `Projects\attribyte\docs\pipedream-proxy-migration-plan.md`.

- **2026-06-03 — ⚠ FABRICMEGASTORE IS NO LONGER A CLIENT (Steve).** The entire CAPI/measurement thread above was built/proven against FabricMegaStore as the live client (workspace 5ab0ffdc on prod `kqpllsenlvdgsznptylb`, real Google Ads data, $16.83 CPA panel). That client relationship has ended — do NOT treat that workspace's data as a current real client, and the Meta/Facebook connection for it is moot ("won't be able to connect Facebook yet"). The proxy code + measurement pipeline remain valid for the NEXT client; FabricMegaStore is just no longer the validation vehicle.
- **2026-06-03 — PIPEDREAM CONNECT PROXY MIGRATION COMPLETE (all eligible adapters).** Extended the Meta proxy pattern (#178) to every proxy-eligible connector so each reads via Pipedream's DEFAULT OAuth client — NO custom client, NO `PIPEDREAM_OAUTH_APP_ID_*`. PRs all MERGED to main: **#178** Meta · **#179** HubSpot+LinkedIn (+ shared `base-adapter.proxyGet` helper) · **#180** TikTok (envelope unwrap) · **#181** Salesforce+Bitrix24 (dynamic-domain → relative-path proxy; built via the `finish-proxy-plumbing` ultracode workflow — 3 parallel convert agents + verify + a code-review agent). `PROXY_PROVIDERS = {meta_ads, hubspot, linkedin_ads, tiktok_ads, salesforce, bitrix24}`. Per-adapter wrinkles handled: LinkedIn needs `LinkedIn-Version`/`X-Restli` headers (via `x-pd-proxy-*` upstreamHeaders); TikTok `{code,message,data}` envelope; SF/Bitrix dynamic domains use RELATIVE target paths (Pipedream resolves the instance host); SF SOQL `nextRecordsUrl` normalized to relative (toRelativePath) to avoid pinning the instance host. **EXCLUDED by design:** Google Ads (streaming `searchStream` + 30s proxy cap → stays custom-client `oa_Wyiyxk`); **Marketo** (per-instance munchkin host + bespoke API-key auth; proxy mode passes empty creds → can't build host; Pipedream can't resolve it → stays direct API-key path); BigQuery/Snowflake/Segment (service-account/API-key/write-only → not Pipedream OAuth). Each PR: typecheck + vitest(50) green + code-review (the one HIGH — SF absolute-cursor — fixed). Plan doc `Projects\attribyte\docs\pipedream-proxy-migration-plan.md` = COMPLETE. Onboarding doc `Downloads\attribyte-meta-read-setup.md` rewritten to "just click Connect" for proxy providers. **RUNTIME VALIDATION STILL PENDING for ALL (no live connection used in any build):** connect a provider via plain Pipedream Connect → `POST /integrations/:id/test` → `/:id/sync`. Confirm: dynamic-domain relative-path resolution (SF/Bitrix), Bitrix GET-vs-POST, LinkedIn `x-pd-proxy-` header stripping, TikTok advertiser-list dual-auth, Meta token injection. **NONE of the 6 proxy adapters has been proven end-to-end on a real connection yet** — first live connect is the gate.

- **2026-06-03 — CATALOG ADAPTER BUILDOUT COMPLETE (PR #182 merged) + Elevar/Stape scoped (#183) + Notion guide.** Steve: "wire ALL the connectors." Built 15 NET-NEW proxy-native adapters (via parallel ultracode workflows + central-registration agent + code-review agent): CRM (pipedrive, close), ads (microsoft_ads — found a REST path, not SOAP), mktg-auto (klaviyo, customer_io, activecampaign), revenue (stripe, chargebee — new `revenue` category), analytics (ga4→campaigns/ad_performance, mixpanel→contacts, posthog→contacts), data-src (typeform, calendly→new `productivity`; intercom, zendesk→new `support`). All proxy-native (inline minimal config, `this.proxyGet`, dynamic-domain→relative path), registered in AdapterType/createAdapter/getSupportedProviders/SYNC_TYPES_BY_PROVIDER/PROXY_PROVIDERS; typecheck+50 tests green. **EXCLUDED w/ reasons:** amplitude (zip export, proxyFetch JSON-only + no person-enum), rudderstack (outbound-only CDP), marketo (per-instance munchkin + bespoke auth — earlier). **Per-integration config to set when connected:** microsoft_ads → `MICROSOFT_ADS_DEVELOPER_TOKEN` env + settings.customer_id(+login_customer_id); ga4/mixpanel → property/project id in settings.customer_id. **Slack/Sheets/Webhook = OUTBOUND** (alerts/export/push), NOT read-sources — pending Steve's yes/no (asked, not answered). GTM already shipped as the read-only Tracking Audit (#169-#172). **STILL the gate: nothing is runtime-validated (no live connection) — all doc-researched.** **KEYSTONE FOLLOW-UP: build the events/activities/touchpoint SINK** — `runSyncType` has no sink for events/activities/leads, so posthog-events/close-leads fetchers + the analytics event paths + Elevar/Stape ALL wait on it; it's the bridge from platform-reported CPA to real multi-touch DDA. Elevar/Stape scoping doc shipped (#183, `docs/server-side-tracking-integrations-plan.md`) — see [[attribyte-tracking-integrations-elevar]]. **Notion visual tech guide created** (GTMVP × AttriByte architecture, mermaid + FAQs + gotchas): https://www.notion.so/GTMVP-AttriByte-Technical-Architecture-Visual-Guide-37441202dff681ab9a03c9b956926c89

Related: [[gtmvp_attribyte_integration]] · [[attribyte_release_prep]] · [[gtmvp_attribyte_ai_attribution]] · [[attribyte_server_llm_path]] · [[attribyte-tracking-integrations-elevar]]
