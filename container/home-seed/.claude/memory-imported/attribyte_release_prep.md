---
name: attribyte-release-prep
description: "Attribyte release-readiness build (2026-06-02) — audit verdict, in-flight PRs, deferred items, and the Atlas embedded-chat build decisions"
metadata: 
  node_type: memory
  type: project
  review_after: 2026-06-09
  originSessionId: ce43c425-e6e3-48a9-9015-78febb55f10f
---

Attribyte release-prep, started 2026-06-02 (Steve: "get this thing ready for release ASAP"). Karpathy-lens audit verdict: **~85% ready, NOT today; blockers are PERIMETER not core.** Product (attribution/ingestion/MMM/integrations/Atlas-backend/billing) is genuinely built, auth-gated, honest (labeled samples, provenance pills), builds 8/8, 50 tests pass, from-zero migration gate fixed. The ~152 "pending" markers are mostly benign (honesty docstrings, labeled sample fixtures, HTML placeholder=).

**SHIPPED:** PR #145 — close-now cleanups (sdk-web TS2774 fix + rebuilt min.js, collector go.sum, --passWithNoTests, dead footer links, stripped unconsumed velocity placeholder, stale CLAUDE.md known-issue removed, 11 env vars documented, unused imports). Build 8/8 green.

**SHIPPED 2026-06-02 — PR #146 MERGED to main** (merge commit 82d958c): the 5 release branches consolidated into ONE release PR (resolved 3 cross-branch overlaps locally: copy∩#143 HomeHero, copy∩trial PricingPage, trial∩entSec index.ts), build-verified 8/8 green, adversarial security review PASSED on all 3 auth/crypto branches (oidc-dns rated strongest). Contents = secure-system-router + oidc-dns-verification + secure-enterprise-router + 14day-trial + honest-compliance-copy. **DB migration `20260602000000_add_workspace_trial.sql` APPLIED to prod Supabase kqpllsenlvdgsznptylb** (additive ADD COLUMN trial_ends_at; trial enforcement middleware fails open so was never a breakage risk; no deploy auto-applies migrations — railway.toml just runs node dist/index.js). Trial now actually enforces. Branch suffix `-FS6Lj` required or push 403s.

**(historical) The 5 in-flight branches** — now all in #146 — were: honest-compliance-copy (soften SOC2/residency to "roadmap" — Steve confirmed NOT certified), 14day-trial (soft enforcement: block writes after trial_ends_at on free, reads stay; NO Stripe object until convert), secure-system-router (auth+admin + collector heartbeat carve + remove fake endpoints), secure-enterprise-router (requireAuth+requireWorkspace, rip out x-organization-id header-trust, #S1=A1 org===workspace no new table), oidc-dns-verification (real jose JWKS sig/exp + real resolveTxt).

**DELIBERATELY DEFERRED (honest, not dropped):** SAML done-right (current regex parser = account-takeover vuln; stays DISABLED; 1-2 days + security review + test IdP; OIDC carries SSO v1); org-hierarchy data model (multi-day vs known-fragile migration chain; org===workspace covers v1). Honest framing: securing the enterprise routes makes them SAFE not FUNCTIONAL — the enterprise tables aren't in prod, so secured routes 401 outsiders / 404 insiders (dormant surface).

**ATLAS embedded-chat (NEW build, Steve 2026-06-02):** Atlas is "missing from the whole platform" as a product surface (backend exists in apps/api, no usable chat UI). Build = a chat function DOCKED INSIDE the dashboard (NOT its own tab), with advanced data-viz output + web access (tools). DECISIONS: **gate = any PAID tier gets full Atlas; trial/free = ~2 queries/week teaser** (tunable; conversion lever). **Model = Kimi 2.6** (current OpenRouter model — NOT Claude). **Backend = OpenRouter on Railway** — Steve wanted his Claude Max sub but that's INFEASIBLE: claude -p can't run on Railway (headless, no Max session, ToS) — see [[attribyte_server_llm_path.md]]. OpenRouter is the sanctioned server path. Grounding confirmed Atlas was UNSURFACED, not missing (runAtlas orchestrator + /atlas/query + AtlasChat.tsx all existed) → Phase 1 was an EXTENSION not a rebuild.

**ATLAS Phase 1 SHIPPED + MERGED 2026-06-02 — PR #147 merged to main (merge commit 100b7be); PR #145 + #146 also merged. Was branch claude/atlas-phase1-dock-FS6Lj.** Docked panel (new ui/sheet.tsx right-anchored Radix Dialog; AtlasDock launcher FAB in AppLayout; AtlasDockContext above route tree so open-state survives nav; re-mounts existing AtlasChat with h-full). Tier gate (middleware/atlas-quota.ts on POST /atlas/query: PAID_TIERS={growth,business,enterprise}=unlimited; free/trial=ATLAS_FREE_QUERY_LIMIT/ATLAS_FREE_WINDOW_DAYS env, default 2/7d; fails open; new table `atlas_query_usage` APPLIED to prod kqpllsenlvdgsznptylb). web_search tool (PAID-ONLY, Steve's call) via OpenRouter `web` plugin on existing OPENROUTER_API_KEY — no new key; withheld from free tool list + handler guard; optional ATLAS_WEB_MODEL. Trust disclaimer in chat empty state (workspace-scoped/read-only/no cross-customer/sources-shown). Build 8/8 green. **Real Atlas tiers = free|growth|business|enterprise (NO `trial` tier — trial = free + trial_ends_at). Atlas model = `moonshotai/kimi-k2.6` (PAID endpoint).** ⚠️ FIXED 2026-06-02: the `:free` variant threw HTTP 429 "temporarily rate-limited upstream" (shared free pool throttled by provider 'Crucible' — NOT account/tools/credits; ~$11 credits remained). First live chat invocation revealed it. Fix = `ATLAS_MODEL=moonshotai/kimi-k2.6` env on Railway celebrated-courage @attribyte/api prod (project c8e3eefe / env d6b01b54 / service d864651e); verified via /atlas/health?probe=true reachable:true + a direct tools call (finish_reason:tool_calls, ~$0.0002/call). **Lesson: OpenRouter `:free` models are unreliable for production (frequent upstream 429s) — use the paid variant (pennies) or BYOK.** Railway MCP: reads work; set_variables returns "Failed to fetch" transport errors but the mutation often LANDS anyway (false negative) — verify before retrying. Prod API also has `NODE_ENV=development` (smell, unrelated, not fixed). Service key + all prod secrets are readable via Railway list_variables.

**⚠️ Latent config bug (flagged to Steve 2026-06-02, NOT yet fixed):** the Railway API env has a Supabase PROJECT MISMATCH — `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` = project `kqpllsenlvdgsznptylb` (correct, where API reads/writes), but `SUPABASE_ANON_KEY` decodes to a DIFFERENT project `vrivnwhpdubkwhdiglyk` (not in Steve's Supabase org list — stale/dead). Could break real self-signups (frontend auth landing in the wrong project = invisible to the API). Verify the live Vercel web app's VITE_SUPABASE_URL/ANON target before trusting self-signup. Prod DB currently has ONLY: user steve@stevekaplan.ai + workspace "Acme SaaS (Demo)" (enterprise) — Atlas answers off this seed data.

**Pending client (deferred by Steve 2026-06-02):** "Fabric Megastore" = a MANUAL-onboard client (not a self-signup). Steve said hold off. To provision when he gives the word: need owner email + plan_tier; steps = INSERT workspace + workspace_members membership (+ optional seed data). No Fabric Megastore record exists in the DB yet. **Brand color = AZURE BLUE (#0070c7/#0c8ee9), NOT purple — existing AtlasChat purple is off-brand.** RESOLVED 2026-06-02: (1) **Visual = "Instrument" theme (dark data-cockpit, graph-paper grid, azure-blue accent, mono telemetry)** — Steve's pick A from 3 mockups; shipped commit f9465bb on PR #147. Replaces the off-brand purple. (2) **Model STAYS free Kimi (`moonshotai/kimi-k2.6:free`)** — Steve declined Claude-via-OpenRouter / Anthropic API on COST (2026-06-02). Therefore the **"never used to train your data" disclaimer line stays OUT permanently** (can't honestly claim it for the free model) — the disclaimer ships with only the verifiable claims (workspace-scoped / read-only / no cross-customer / sources-shown). **claude -p / Max sub for the PRODUCT chatbot = NOT viable** (technical: Railway is headless, no CLI/Max session; ToS: Max is personal-use, not for serving customers) — confirmed to Steve again 2026-06-02; product Claude use would require Anthropic API or Claude-via-OpenRouter, both declined on cost. The "always claude -p, never API" rule governs OUR automation, NOT a shipped product. **Phase 2 BUILT + PR #148 (review-only, OPEN, not merged) 2026-06-02 — shared live conversation across dock+/atlas + token streaming.** Built in an ISOLATED git worktree off 100b7be (main checkout was contended by an active agent — see below), 8/8 green, 3-agent adversarial review with all HIGH/MED fixed (write-guards+heartbeat+abort on SSE, synchronous in-flight lock, reset-aborts-stream, finalized-guard, UUID ids, stable mutateAsync). New `runAtlasStream` generator (single source of truth; runAtlas = thin consumer so /query byte-identical) + `POST /atlas/query/stream` SSE + `AtlasConversationProvider` + `useNLQueryStream` (fetch+ReadableStream, not EventSource — auth headers). Worktree cleaned up after push (junction-removed-then-worktree-remove, main node_modules verified intact). ⚠️ **An active "FieldHelp / contextual ? help tooltips" agent committed a ~100-file rollout to LOCAL main (commit fc72bcd) but has NOT pushed to origin (origin/main still 100b7be).** **CONFLICT RESOLVED 2026-06-02:** rebased #148 onto fc72bcd (force-pushed, tip 5e7e985) in an isolated worktree. The ONLY overlap was AtlasChat.tsx (App.tsx was never touched by FieldHelp); it AUTO-MERGED clean (FieldHelp's insertions sat at distinct anchors) — verified line-by-line + builds 8/8: final AtlasChat has Phase2's streaming structure + all 8 FieldHelp tooltips, no ghost nlQuery refs. #148 now CONTAINS fc72bcd in its history, so its GitHub diff shows FieldHelp's ~100 files UNTIL fc72bcd is pushed to origin/main. To land (either, both conflict-free): (1) push local main (FieldHelp/fc72bcd) to origin → #148 becomes a clean Phase2-only diff + fast-forward merges; or (2) merge #148 as-is to bring FieldHelp + Phase2 to main together. Pushing fc72bcd (the agent's ~100-file FieldHelp rollout) to origin/main = a real deploy — Steve's call, not done. Phase 3+ not scoped.

Related: [[gtmvp_attribyte_ai_attribution.md]], [[attribyte_server_llm_path.md]], [[feedback_verify_clean_build_first.md]].
