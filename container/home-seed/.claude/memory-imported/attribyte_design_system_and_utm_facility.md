---
name: attribyte-design-system-and-utm-facility
description: "AttriByte's real design-system kit + tokens, the UTM Facility prototype (shipped PR #165), and the cardinal 'explanations everywhere' product-UX principle."
metadata:
  node_type: memory
  type: project
  originSessionId: 19628f7d-c9d4-4465-b2ba-b6931891789e
---

**🔄 UPDATE 2026-06-06 — SIGNAL LEDGER REBRAND (verified against origin/main).** A later Claude rebranded **`apps/marketing` ONLY** (PR #185 `rebrand/signal-ledger-marketing`, merged to main). The marketing canonical tokens are now in `apps/marketing/src/index.css` + `tailwind.config.js`: **primary `#1A56DB`** (ramp 50 `#EBF2FF` → 600 `#1A56DB` → 700 `#1447BA` → 800 `#0E3599`), **fonts Instrument Serif (display, 24px+) + Inter (body) + JetBrains Mono**, **white-first slate neutrals** (border `#e2e8f0`), radius 4/6/10/16, semantics `#15803D`/`#B45309`/`#B91C1C`. **`apps/web` (the product at app.attribyte.xyz) was NOT rebranded — still the old shadcn azure theme** (`--primary: 207 90% 54%`, dark slate-900 shell, `#0c8ee9` family below). So the `#0c8ee9` tokens below are still accurate for the PRODUCT app but stale for the marketing site. ⚠️ Their `apps/marketing/tailwind.config.js` has a latent bug: `success/warning/danger` utilities (`#10b981`/`#f59e0b`/`#ef4444`) don't match the design's own `index.css` tokens (`#15803D`/`#B45309`/`#B91C1C`). Two cleaned standalone design systems built at `C:\Users\User\Downloads\While Claude Was Sleeping 6-5-26\Design System\{attribyte,gtmvp}\` — the attribyte one matches the shipped Signal Ledger marketing tokens exactly; gtmvp is greenfield (`#F97316` Operator Forge, dark-first, Archivo Black + DM Sans, not yet shipped). See [[gtmvp-repo-canonical-paths]].

**AttriByte Design System kit** — Steve provided it via an Anthropic design-share URL (`api.anthropic.com/v1/design/h/...?open_file=ui_kits/product/index.html`) that returns a **gzip tarball**. Extracted to `C:\Users\User\Downloads\attribyte-design-kit\attribyte-design-system\`. Source of truth = `project/colors_and_type.css` (tokens) + `project/DESIGN_SYSTEM.md` + `project/ui_kits/product/` (React UI-kit recreation of `apps/web`).

**Real tokens (use these, NOT a custom aesthetic):** primary `#0c8ee9` (attribyte-500), hover `#0070c7`; ink `#0b1424`, muted `#475569`; slate scale; border `#e2e8f0`; bg white / page `slate-50`. **Product app uses `--font-product` = SYSTEM font (no Inter); marketing uses Inter; mono = JetBrains Mono.** Radius 8px (shadcn `--radius`). **Product shell = DARK `slate-900` sidebar + `attribyte-500` 3px active indicator + light slate-50 content + white ProdCards + 2px-left-accent stat cards + 24px page headings.** Logos in `project/assets/logo-*.svg`.

**🔴 CARDINAL PRODUCT-UX PRINCIPLE (Steve, 2026-06-02, said twice):** the AttriByte product must have **explanations + tooltips EVERYWHERE** — plain-English, never overwhelming. This IS the kit's "Simplification Plan" (move from advanced-operator tool to something a non-technical client/Steve-for-them can navigate): `IntroCard` ("What you're looking at"), `PlainEnglishSummary` ("In plain English…"), `RunAnalysisBar`, hover tooltips on every jargon term, Atlas as a ⌘K launcher, 5-group nav (Overview/Insights/Pipeline/Connect/Settings). Apply to ALL AttriByte UI work. Steve got "confused and overwhelmed" without it.

**Lesson:** the `/frontend-design` skill pushes "bold/distinctive" — for a feature INSIDE an existing product, that's wrong; **match the existing brand/site.** I built a warm-paper editorial aesthetic first; Steve hated it ("doesn't match the site"). Content/ideas were "A++"; only the skin was wrong.

**UTM Facility** — new feature, Connect → UTM links. 3 views: **Taxonomy** (locked dictionary of canonical source/medium/campaign + aliases + governance), **Link Builder** (compose tagged links from the dictionary, live-validated, channel shown), **UTM Linter** (scan live UTMs, map variants→canonical, quantify mis-bucketed spend). Closes GTMVP audit finding #12 ("UTM taxonomy is likely inconsistent"). **Prototype SHIPPED: PR #165 → `prototypes/utm-facility/`** (static HTML spec). Mockups also at `C:\Users\User\Downloads\attribyte-utm-mockups\`. **PRODUCTION FEATURE SHIPPED: PR #168 (squash-merged to main, deploys Railway+Vercel).** Route/nav: Connect → UTM Links (`/utm-links`). API (`apps/api`): `lib/utm-taxonomy.ts` (per-workspace dict in `workspaces.settings.utm_taxonomy` + defaults + alias matching), `routes/utm.ts` → `GET/PUT /utm/taxonomy`, `GET /utm/lint` (RPC `lint_utm_values` GROUP-BYs touchpoints source/medium/campaign → variants vs taxonomy → health), `POST /utm/lint/apply` (UPDATE touchpoints variants→canonical + record aliases). Web (`apps/web`): `UtmLinksPage` (3 tabs: Taxonomy editor / Link Builder / Linter) using real shadcn + the Simplification guide components (`PageTooltip`, `AnalysisGuide`, `PlainEnglishSummary`, `EmptyState`, `FieldHelp`), `hooks/use-utm.ts` React-Query hooks. Migration 20260603050000 (RPC) applied to prod. **Also fixed PR #167:** contacts UNIQUE(workspace_id, external_id) (the "other 1", same class as campaigns #164). Linter shows empty state until real tagged touchpoints flow (demo workspace was purged clean).

**GTM TRACKING AUDIT — SHIPPED (PR #169, sibling feature).** Steve's idea: connect Google Tag Manager + audit site container tags (DIFFERENT from UTM link tags — it's the GA4/Ads/Pixel/consent scripts on the page). Lives in **Tracking Health** (NOT the UTM Facility) — a new "GTM tag audit" section in `TrackingHealthScorecard` on the Integrations page. Read-only (`tagmanager.readonly`). API: `GET /tracking-health/gtm-audit` → `apps/api/src/health/gtm-audit-service.ts` reads GTM `versions:live` (3 calls: /accounts → /containers → versions:live), classifies tags (googtag=GA4, awct=Ads conversion, gclidw=Conversion Linker, html+fbevents=Meta Pixel, ua=legacy, consentSettings/consentInit=consent), scores findings (crit −25 / warn −10). Connection = existing Pipedream pattern: `gtm: 'google_tag_manager'` slug + INTEGRATIONS entry. **RUNTIME PREREQ (Steve/partner action, same as Google Ads `oa_Wyiyxk`): set env `PIPEDREAM_OAUTH_APP_ID_GTM` = a custom Google OAuth client with `tagmanager.readonly` scope, in Pipedream proj `proj_VbsZ3BV`.** Until set, the section shows a connect prompt + the token won't export. NO adapter, NO migration. Design doc: `docs/gtm-tracking-audit-design.md`. Phase 2 (later): pinned container UI, draft-vs-live drift, scheduled re-scan + history, audit PDF. Why UTMs matter: `touchpoint-creator.ts` `inferChannel`/`getChannelFromUtm` derives every touchpoint's channel from utm_source/medium → messy UTMs mis-bucket attribution → corrupt the CPA the guarantee rides on.

**Demo video SHIPPED LIVE (PR #166).** 46s screen-only walkthrough of the real prototype + VO + burned-in captions. Pipeline (reusable): Chrome `--headless` screenshots of the 4 views (`--virtual-time-budget` so reveal anims finish; fresh `--user-data-dir` or it attaches to a running Chrome and writes nothing) → .NET trim-to-content + caption/endcard PNGs (drawtext segfaults on this box, so PNG overlays) → ffmpeg `crop` scroll-pans + overlay + concat → ElevenLabs VO (voice "Adam" pNInz6obpgDQGcFmaJgB; env ELEVENLABS_VOICE_ID is the generic readback voice, NOT a narrator). Assets in `Downloads\attribyte-utm-mockups\` (`utm-facility-demo.mp4`, `vo.mp3`, `shots\`). **LIVE on marketing `/product` "See it in action" section** (`apps/marketing/public/` + `ProductPage.tsx`) → Vercel. **⚠️ HONEST FLAG (told Steve):** it's on the LIVE public product page but the feature is a PROTOTYPE not yet built in `apps/web`; Steve explicitly directed it live ("get it live on the page. Without asking for permission.").
