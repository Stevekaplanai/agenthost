---
name: attribyte-recovered-honesty-prs
description: "3 draft PRs of never-merged Attribyte honesty/integrity work, recovered from stranded local branches 2026-06-01, pending Steve's review"
metadata: 
  node_type: memory
  type: project
  review_after: 2026-06-15
  originSessionId: ce43c425-e6e3-48a9-9015-78febb55f10f
---

During the 2026-06-01 Projects/ + git cleanup, found 6 never-PR'd local Attribyte branches. 3 were verified superseded and deleted (fix-migration-chain → baseline reset won; touchpoint-pipeline → v2 merged; hero-video → tour-audio merged). The other 3 carried genuinely unmerged work that existed ONLY locally (at risk), so they were pushed to GitHub as DRAFT PRs for Steve to review/rebase/merge-or-close:

- **PR #133** `claude/integrity-honest-data-FS6Lj` (+1652/-894, 19 files) — replaces fabricated random-data surfaces with honest states across monitoring, velocity, MMM/incrementality disclosure, explainability, system-perf; + decision log + customer-trust-kit drafts. The biggest one. https://github.com/Stevekaplanai/attribyte/pull/133
- **PR #134** `claude/privacy-gate-and-integration-truth-FS6Lj` (+458/-50) — consent/GPC gating of fingerprint identity + PII in ingestion (consented behavior unchanged); honest two-tier integration labels. https://github.com/Stevekaplanai/attribyte/pull/134
- **PR #135** `fix/build-and-lookalike-honesty` (+173/-165) — gates lookalike fabrication + resolves @attribyte/features build error. https://github.com/Stevekaplanai/attribyte/pull/135

UPDATE 2026-06-01 (later same day): all 3 REBASED onto current main, pushed, undrafted, and **merge-ready** — mergeable=MERGEABLE, Vercel attribyte-web + attribyte-marketing builds PASS on all three, GitGuardian clean. Resolution principle on #133: kept main's newer/real implementations, re-applied only the branch's unique honest-data work (explainability.ts honest-LIME + unified-measurement backend); measurement services + pages + trust-kit docs resolved to main (main already did it better, incl. labeled-sample fallbacks per [[feedback_never_blank_even_when_live.md]]). #134 dropped 3 doc commits already in main, replayed 2 (privacy-gate + integration labels). FOLLOW-UP SHIPPED as PR #136 (https://github.com/Stevekaplanai/attribyte/pull/136, open + mergeable, adversarially reviewed, apps/api typecheck-clean): the mmm-service per-field provenance feature — optional `methodology` map on ChannelContribution labeling each field 'derived' vs 'heuristic_estimate' (confidenceInterval + optimalSpend = heuristic; the rest = derived). Optional field = zero breakage; UI surfacing deferred (mockup-first per [[feedback_ui_mockups_before_committing.md]]). PRE-EXISTING BUG found during review, now FIXED in PR #137 (https://github.com/Stevekaplanai/attribyte/pull/137, open + mergeable, typecheck-clean): the carryover/immediate split in mmm-service `calculateChannelContributions` was INVERTED — code set `carryoverFraction = λ` (decayRate), but the adstock weight `(1−decayRate)^lag` implies immediate share = λ and carryover = 1−λ (also matches AdstockParams.decayRate doc "lower = longer carryover"). Fix swaps them. Values only; #136's provenance labels unaffected. CAVEAT: apps/api (backend) is not built by PR CI (Railway deploys post-merge to main); agents' local tsc was clean except env-only missing stripe/resend. Final merge to main left for Steve.

(historical) These were stale vs main (from ~05-29/05-30) and needed rebase before merge. They strongly align with Steve's honest-data / "never fabricate / honesty-guard with sample badges" stance — worth reviewing, not auto-closing. **Why:** this honesty work was branched, committed, then skipped without ever being PR'd; it would have been lost on a reclone. **How to apply:** when revisiting Attribyte, triage #133/#134/#135 — rebase + merge the parts still relevant, or close with a note. Related: [[gtmvp_attribyte_ai_attribution.md]], [[feedback_never_blank_even_when_live.md]].
