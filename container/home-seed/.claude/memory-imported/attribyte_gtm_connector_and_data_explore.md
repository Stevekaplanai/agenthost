---
name: attribyte_gtm_connector_and_data_explore
description: "Attribyte GTM connector tile shipped (#172) + verify-button fix (#171); QUEUED task to explore FabricMegaStore data coverage once GTM connects"
metadata: 
  node_type: memory
  type: project
  originSessionId: 19628f7d-c9d4-4465-b2ba-b6931891789e
---

Attribyte GTM tag-audit connector. The API was fully wired in PR #169 (INTEGRATIONS.gtm with name/category/scope tagmanager.readonly, slug map `gtm` -> `google_tag_manager`, `PIPEDREAM_OAUTH_APP_ID_${provider.toUpperCase()}` -> PIPEDREAM_OAUTH_APP_ID_GTM, gtm-audit-service reads `integrations` where provider='gtm', connect-session/connect-complete use `provider: z.string()` so no allowlist blocks it) — but the WEB Integrations page renders a hardcoded `INTEGRATION_PROVIDERS` catalog in `apps/web/src/pages/IntegrationsPage.tsx` that had no GTM tile, so there was no Connect button. Fixed 2026-06-02: added the `gtm` tile (Analytics category, color #246FDB) next to GA4 — **PR #172 (f64a919)**. Tiles render a colored-circle + initial; the `/integrations/*.svg` logo paths are vestigial (no files exist on disk).

Also fixed 2026-06-02: the tracking **"Verify installation"** button on Settings -> Tracking Code was a dead end (`navigate('/dashboard')`, stale comment "events has no read endpoint"). Now calls `GET /tracking-health` and renders an inline live/no-events result (Tag is live + event count + last-event time, or "No events received yet"). Honest: hasData reflects real events only, never sample. **PR #171 (e9b9197)**.

Gating to connect GTM end-to-end: BOTH deploys must be green — Railway `/api` (env var PIPEDREAM_OAUTH_APP_ID_GTM, Steve set it 2026-06-02; `oa_...` custom-OAuth-client id from Pipedream proj_VbsZ3BV, NOT the Google client id) AND Vercel web (#172). Then app.attribyte.xyz -> Integrations -> Analytics -> Google Tag Manager -> Connect -> popup -> close. NEVER hit Disconnect (wipes config).

**QUEUED TASK (Steve, 2026-06-02):** Once GTM is connected, run as much data exploration (/explore-data) as possible on the FabricMegaStore workspace to figure out (1) how much data we're ingesting from the client RIGHT NOW, and (2) how much we can ingest RETROACTIVELY (backfill). Key facts: pixel/`events` data CANNOT be backfilled (only exists from pixel-install forward). Google Ads, Shopify, and GA4 historical data CAN be pulled retroactively via their APIs (GAQL date ranges / Shopify orders since store creation / GA4 historical). GTM itself is a read-only tag audit, NOT data ingestion — it reveals what's instrumented on the site, informing what COULD flow. Most of this exploration is NOT actually gated on GTM (ingested sources = Shopify + Google Ads + pixel, already connected on FabricMegaStore); offered to start the current-coverage + backfill analysis early. Supabase prod kqpllsenlvdgsznptylb; key tables: events, touchpoints, ad_spend, integrations, contacts, accounts.

**DATA-EXPLORE FINDINGS (2026-06-03, workspace 5ab0ffdc-6491-4c21-a310-817005bfbbbc = Fabric Megastore):** The ONLY real source flowing is Google Ads (active, hourly sync, integration id 49e17b59-c106-4745-be15-2c90232be2ea, customer 1857683050 = Steve's GTMVP account used as the FMS stand-in). events=0, touchpoints=0 (pixel NOT firing), contacts/accounts/opps=0. hubspot + meta_ads integrations are broken DEMO seeds (settings `_demo` key, status error). NO Shopify integration exists (the earlier "Shopify connected" belief was WRONG). conversion_source='platform' so the CPA guarantee is currently measured purely off Google's own reported conversions (circular — no independent first-party measurement). Diagnostic target CPA=11.2; expected channels facebook/google/pinterest (only google flowing).

**GOOGLE ADS BACKFILL DONE (2026-06-03):** Backfilled 31 days -> 156 days (Dec 5 2025 -> Jun 2 2026) by resetting the sync cursor (UPDATE integrations SET last_sync_at = now()-interval '180 days'); the 60s scheduler auto-ran the incremental pull (modifiedSince=cursor) with ZERO deploy/auth/code change. ad_spend 181->818 rows, $34.5K->$122.8K spend, 2,060->9,398 conv. (Reusable backfill trick: reset last_sync_at to force a deeper incremental pull; do NOT null it — null triggers reconstructHistoricalTouchpoints. The 30-day cap at integration-sync-service.ts:281 only bites full/first syncs where modifiedSince is undefined — a known limitation, left as-is per surgical rule.) **KEY INSIGHT: efficiency decayed as spend scaled** — CPA $9.20 (Dec) / ~$11.7-12 (Jan-Mar @ $14-20K/mo) -> $16.17 (May @ $34.7K/mo); ROAS 5.4x -> 3.67x. May spent 17% MORE than April for 7% FEWER conversions = scaled past the efficiency frontier (~$20-25K/mo). Validates the $11.20 target as ACHIEVED (Jan-Mar), not aspirational. All platform-reported (Google's own numbers).

**LIVE-MODEL AUDIT + PLANNING CHAIN (Steve, 2026-06-03):** Launched workflow `attribyte-live-model-audit` (run wf_e39df8dc-a19) — 16 model-family auditors + honesty critic — testing which attribution/predictive models actually produce real output on FMS live data (only ad_spend/campaigns populated). A-priori verdicts: DEAD (starved, 0 inputs) = all attribution (touchpoints=0), velocity/churn/committee/propensity/next-best-action (CRM=0), incrementality (no experiment), lookalike. STRUCTURAL FAIL = MMM (single channel = unidentifiable). LIKELY WORKS (ad_spend-fed) = forecasting, CAC, anomaly, channel-perf/CPA-guarantee (cpa snapshot already exists); optimizer degenerate (1 channel). Synthesize audit into a reusable **Model Data-Requirements Matrix** (per model: min viable data + structural caveats + turn-on trigger = the what-to-instrument-per-client playbook). **THEN (chained, after audit finishes): run a 2nd workflow to PLAN — not implement — all plumbing fixes/builds so a NEW client onboarding lights up the models (source connections incl. Shopify orders + pixel + CRM + multi-channel ads, event/touchpoint ingestion + historical reconstruction, conversion ingestion + conversion_source, identity resolution, onboarding/first-data UX + honest insufficient-data gating, per-model turn-on triggers). Output = build-ready prioritized plan for Steve's greenlight, NOT auto-build.**

**AUDIT RESULT (2026-06-03, wf_e39df8dc-a19 complete):** EXACTLY ONE model produces real output on FMS live data = CPA-guarantee scoreboard + top-line blended CPA/ROAS (read ad_spend; measured CPA $13.97 vs $14.00 baseline, ROAS 3.43). All others starved (read empty touchpoints/CRM). STRUCTURAL ISLAND: ad_spend is read ONLY by the CPA/top-line path; every other model reads `touchpoints` and never ad_spend, so the one populated source feeds almost nothing. **3 TRUE FABRICATION BUGS (must fix before any demo — violate never-fabricate):** (1) MMM — no train gate, 0 conversions -> calculateR2 returns 1.0, writes a "perfect" trainR2=1.0 model. (2) Churn — reads no DB, scores any body off hardcoded weights + GET /predict/churn/model returns invented metrics (acc 0.78, AUC 0.82, n=881). (3) Unified measurement — no gate, generateInsights emits "High agreement... Attribution data is reliable" on ZERO data. Plus ungated silent-zeros (rules attribution returns {}, per-channel table ignores ad_spend, velocity /predict endpoints, incrementality time_based, NBA), buying-committee 404s instead of empty-state, and 3 BROKEN RPCs vs live schema (get_conversion_training_data->missing `conversions` table; get_channel_time_series_data->missing `t.touchpoint_type` + `channel_budgets`) that currently fail INTO honest gates by luck. Honestly-gated already: DDA(100), Custom(200), CAC(10), Forecasting(30d), Lookalike(200), Anomaly, Optimizer, Conversion-propensity. Also: ad_spend.channel 100% NULL; campaigns.budget all 0. **PLANNING WORKFLOW LAUNCHED wf_fc561c70-be9** (Phase 0 honesty hardening -> Phase 1 wire ad_spend into channel views -> Phase 2 pixel->events->identity->touchpoints spine -> Phase 3 CRM/Shopify) — build-ready plan for Steve's greenlight, NOT auto-build.

**PHASE 0 HONESTY HARDENING SHIPPED (2026-06-03, "Act. Don't queue."):** PR #173 (MMM trainable-target gate kills R2=1.0 fabrication; churn metrics=null/benchmark-untrained + `trained` flag; unified generateInsights guarded on contributions.length>0), PR #174 (velocity predict/lead/compare/model-metrics gated via velocityDataGate()+rawPredict), PR #175 (committee/deal-thread `not found` -> NotFoundError 404 incl. the :492 site the plan missed; rules attribution /report -> insufficient-data when 0 converting touchpoints). All squash-merged to main, tsc clean each. REMAINING Phase 0 (small, lower-severity): incrementality time_based empty-partition gate (moot — 0 tests); fix `get_channel_time_series_data` RPC (stale: refs nonexistent t.touchpoint_type/t.revenue/channel_budgets) ONLY co-sequenced with a channel-perf gate, never before (else honest-empty flips to real-zeros); getChannelPerformance silent-zeros gate. Defer the other 2 stale RPCs (get_channel_performance_stats, get_conversion_training_data — they fail safe into gates). **THE BIG UNLOCK (plan critic's #1, NEEDS STEVE'S CALL):** the first-party spine is 100% DARK at the perimeter — `apps/api/src/index.ts:87-101` is a hard CORS allowlist with NO public-ingestion exception, so even a firing pixel is blocked. THIS alone could explain Fabric's 0 events (before even the missing pixel install). Opening it = "1 model works" -> "whole engine lights up", BUT must ship WITH consent/GPC gating (the unmerged honesty PRs #133/#134/#135 — never ship a wildcard tracking spine that ignores consent). Also verify the tracking subdomain DNS first (2.2 before 2.1). Strategic + privacy-sensitive = Steve decides.

See [[gtmvp_attribyte_handoff_capi_build.md]] and [[attribyte_recovered_honesty_prs.md]].
